# Custom Hooks (`src/hooks`)

This directory contains custom React hooks that encapsulate business logic and state management. By extracting this logic into hooks, we can reuse it across multiple components and keep our components clean and focused on rendering the UI.

## Hooks

*   **`useAuth.js`:** A custom hook for managing user authentication. It provides the current user's authentication state, user profile, and methods for logging in and out.
