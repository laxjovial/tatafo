# Styles (`src/styles`)

This directory contains global CSS styles and variables that are used throughout the application.

## Files

*   **`global.css`:** This file contains global styles that are applied to the entire application, such as base font sizes, colors, and background styles. It is imported into the main `index.js` file.
