# Assets (`src/assets`)

This directory is for static assets that are imported into the React application, such as images, icons, and fonts.

For example, you might have:

*   `logo.svg`
*   `user-avatar-default.png`
*   `icons/`
    *   `close.svg`
    *   `menu.svg`
