# Utilities (`src/utils`)

This directory is for general utility functions that can be used throughout the application. These functions should be pure and have no side effects.

Examples of utility functions that might be placed here include:

*   Date formatting functions
*   Data validation functions
*   String manipulation functions
